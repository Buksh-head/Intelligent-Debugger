# Migrations

Place ordered migration files here once a migration tool is chosen
(e.g. Alembic for the FastAPI backend). `schema/schema.sql` is the
current source of truth for local development until migrations are
introduced.
